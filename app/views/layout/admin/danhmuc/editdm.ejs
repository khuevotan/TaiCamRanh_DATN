<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit danhmuc - <%= danhmuc.title %></title>
</head>
<body>
    <h2>Edit danhmuc</h2>

    <% if (typeof status !== 'undefined') { %>
        <%if (status == 'success') { %>
            <div style="color: green;">
                Updated Successfully
            </div>
        <% } else { %>
            <div style="color: red;">
                Some error occurred while editing the danhmuc
            </div>
        <% } %>
        <br>
    <% } %>
    <form action="/danhmuc/<%= danhmuc.id %>" method="post">
        <input type="hidden" name="_method" value="put">

        <label for="title">Title</label>
        <input type="text" id="title" name="title" value="<%= danhmuc.title %>">
        <br> <br>
        <label for="description">Description</label>
        <input type="text" name="description" id="description"  value="<%= danhmuc.description %>">
        <br> <br>
        <label for="published">Publish</label>
        <input type="checkbox" name="published" id="published" <% if (danhmuc.published) { %> checked <% } %>>
        <br><br>
        <button type="submit">Update</button>
    </form>
</body>
</html>